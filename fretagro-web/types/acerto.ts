// types/acerto.ts — re-exports from @fretagro/types (source of truth: packages/shared)
// Keep this shim so existing @/types/acerto imports continue to work unchanged.
export * from '@fretagro/types'
